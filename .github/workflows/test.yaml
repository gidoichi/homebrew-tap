name: Test
on:
  push:
  workflow_dispatch:
jobs:
  test-brew-install:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@6fa4c8300154020277b11ecfbab85263ca1d5a5a # master
      - name: brew install --build-from-source
        env:
          HOMEBREW_NO_INSTALL_FROM_API: "1"
        run: |
          set -eux
          for pkg in $(ls $(brew --repository gidoichi/tap)/Formula | sed 's/\.rb$//'); do
            brew install --build-from-source --verbose "${GITHUB_REPOSITORY}/${pkg}"
          done
